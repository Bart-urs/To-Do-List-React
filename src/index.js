import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Możesz usunąć ten import, jeśli usunąłeś plik index.css
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);